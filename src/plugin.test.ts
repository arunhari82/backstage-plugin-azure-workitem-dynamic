import { azureWorkitemDynamicPlugin } from './plugin';

describe('azure-workitem-dynamic', () => {
  it('should export plugin', () => {
    expect(azureWorkitemDynamicPlugin).toBeDefined();
  });
});
